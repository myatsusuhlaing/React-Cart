import React from 'react';

import data from './data';
const App = () => {
  // {data.productData.map((item,index) =>{
  //   return(
  //     <Card title={item.title} desc={item.desc} price={item.price} key={index}/>
  //   )
  // })}
};

export default App;